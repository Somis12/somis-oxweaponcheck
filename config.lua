config = {}
config.relaxedmode = true -- if false, the script will check every shot, if true, it will check every shot in 30 seconds (changeable down here). to save performence.
config.relaxed_timer = 30000 -- if using relaxed, how much ms wait before running another check.